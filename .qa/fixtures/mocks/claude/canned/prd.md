## Problem Statement

This is a canned PRD artifact produced by the QA gate mock agent.
It exists to verify the full orchestration loop runs without errors.

## Success Criteria

- The orchestrator completes the feature lifecycle without crashing
- All phase transitions are recorded in the state directory
- Artifacts pass schema validation
