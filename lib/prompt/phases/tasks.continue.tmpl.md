Now produce the task list for **{{FEATURE_ID}}: {{FEATURE_TITLE}}**.

Output to `{{MONOZUKURI_WORKTREE}}/tasks/prd-{{FEATURE_ID}}/tasks.json` following `schemas/tasks.schema.json`.

The PRD and TechSpec you wrote are in this session — do not re-read them from disk.

Each task must have: id, title, description, files (array), status ("pending").
