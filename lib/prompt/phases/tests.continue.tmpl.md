Now write and run tests for the implementation you produced for **{{FEATURE_ID}}: {{FEATURE_TITLE}}**.

Write test files to `{{MONOZUKURI_WORKTREE}}` following the project's existing test conventions. Run the test suite and confirm it passes.

The implementation context is in this session — do not re-read prior artifacts from disk.
